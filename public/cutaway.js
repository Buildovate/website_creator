document.querySelectorAll('.cutaway').forEach(section=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)'),tour=section.querySelector('[data-cutaway-tour]'),overlay=section.querySelector('[data-cutaway-overlay]');let timer=null,selected=0;const set=i=>{selected=i;section.querySelectorAll('[data-hotspot]').forEach(x=>x.setAttribute('aria-pressed',String(Number(x.dataset.hotspot)===i)));section.querySelectorAll('[data-layer]').forEach(x=>x.classList.toggle('active',Number(x.dataset.layer)===i));const label=section.querySelector('.cutaway-notes [data-hotspot="'+i+'"] b').textContent;section.querySelector('[data-cutaway-status]').textContent=label+' selected.'};const stop=()=>{clearInterval(timer);timer=null;tour.setAttribute('aria-pressed','false');tour.textContent=reduced.matches?'Next detail':'Play system tour';section.classList.remove('tour-playing')};section.addEventListener('click',event=>{const button=event.target.closest('[data-hotspot]');if(button){stop();set(Number(button.dataset.hotspot))}});tour.onclick=()=>{if(timer){stop();return}if(reduced.matches){set((selected+1)%section.querySelectorAll('[data-layer]').length);return}tour.setAttribute('aria-pressed','true');tour.textContent='Pause system tour';section.classList.add('tour-playing');timer=setInterval(()=>set((selected+1)%section.querySelectorAll('[data-layer]').length),2800)};overlay.onchange=()=>section.classList.toggle('guides-hidden',!overlay.checked);reduced.addEventListener('change',stop);if(reduced.matches)stop();document.addEventListener('visibilitychange',()=>{if(document.hidden)stop()});if('IntersectionObserver'in window)new IntersectionObserver(entries=>{if(!entries[0].isIntersecting)stop()}).observe(section)});
