import {e} from './core.js';

export function heroContact(s,{base='',preview=false}={}) {
 const digits=String(s.phone||'').replace(/\D/g,'');
 const phone=digits.length>=10?`<a class="hero-contact-phone" href="tel:+${digits.length===10?'1':''}${digits}">${e(s.phone)} <span aria-hidden="true">↗</span></a>`:'';
 return `<aside class="hero-contact" aria-label="Contact ${e(s.name)}"><div class="hero-contact-heading"><span>LET’S TALK ABOUT YOUR PROJECT</span><h2>How can we help?</h2>${phone}</div><form data-lead-form data-slug="${e(base.split('/').pop())}" data-project="" data-preview="${preview}"><div class="hero-contact-row"><label>Your name<input name="name" autocomplete="name" required minlength="2" maxlength="120" placeholder="Full name"></label><label>Phone number<input name="phone" type="tel" autocomplete="tel" required maxlength="40" placeholder="Your phone number"></label></div><label>What can we help with?<select name="service"><option value="">Choose a service</option>${(s.services||[]).map(x=>`<option>${e(x.name)}</option>`).join('')}</select></label><label>Tell us a little more<textarea name="message" rows="2" maxlength="2500" placeholder="Your project, location, or question"></textarea></label><input class="hero-contact-hp" name="website" tabindex="-1" autocomplete="off" aria-hidden="true"><label class="hero-contact-consent"><input type="checkbox" name="consent" required>I agree to be contacted about this request.</label><button type="submit" ${preview?'disabled':''}>Submit my request <span aria-hidden="true">↗</span></button><p data-lead-status role="status">${preview?'Preview only — submissions are disabled.':''}</p></form></aside>`;
}
